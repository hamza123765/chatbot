[build]
  # For a pure API, this can just be an empty directory or your serverless folder
  functions = "functions" 

[[redirects]]
  from = "/api/*"
  status = 200
  # Points to your API entry file (e.g., functions/api.js)
  force = true
  to = "/.netlify/functions/api/:splat"
